// removed: deployment now uses the gh-pages CLI via package.json scripts
// kept as a stub for compatibility; create .nojekyll via the predeploy script instead.
module.exports = function noop() {
  // noop
};
